setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: faq-server/Dockerfile
    c_558591694114_2: whatsapp-client/Dockerfile
    c_5527981359682_2: whatsapp-client/Dockerfile

  config:
    NODE_ENV: production    

