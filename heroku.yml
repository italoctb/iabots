setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    faq: infra/dockers/whatsapp.Dockerfile
    whatsapp: infra/dockers/whatsapp.Dockerfile

  config:
    NODE_ENV: production

