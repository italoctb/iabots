setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    faq: infra/dockers/server.Dockerfile
    whatsapp: infra/dockers/whatsapp.Dockerfile

  config:
    NODE_ENV: production

