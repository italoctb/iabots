setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    faq: 
      context: . 
      file: infra/dockers/server.Dockerfile
    whatsapp: 
      context: .
      file: infra/dockers/whatsapp.Dockerfile

  config:
    NODE_ENV: production

