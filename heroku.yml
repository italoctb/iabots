setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: faq-server/Dockerfile
    c_558599169414_2: whatsapp-client/Dockerfile

  config:
    NODE_ENV: production

