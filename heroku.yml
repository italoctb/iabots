setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: faq-server/Dockerfile
    c.558599169414.2: whatsapp-client/Dockerfile

  config:
    NODE_ENV: production

